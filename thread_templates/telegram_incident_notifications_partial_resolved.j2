update: some alerts <b>resolved</b>{%- set curr = payload.alerts | selectattr('status', 'equalto', 'firing') | map(attribute='labels.instance') | list -%}
{%- set removed = previous_payload.alerts | selectattr('status', 'equalto', 'firing') | map(attribute='labels.instance') | reject('in', curr) | unique | list -%}
{%- if removed %} ({{ removed | join(', ') }}){% endif %}
