:loudspeaker: webhook *{{ step.value -}}*
{%- if step.status == 'ok' -%}
 ({% if step.result < 400 %}{{ step.result }}{% else %}<https://docs.impulse.bot/stable/warnings/ResponseCode/|{{ step.result }}>{% endif %})
{%- elif step.status == 'Timeout' -%}
 (<https://docs.impulse.bot/stable/warnings/TimeoutError/|TimeoutError>)  |  :loudspeaker: admins ({%- for u in users.values() if u and 'admin' in u.roles %}<@{{ u.id }}>{% if not loop.last %},{% endif %}{% endfor -%})
{%- else -%}
 (<https://docs.impulse.bot/stable/warnings/ConnectionError/|ConnectionError>)  |  :loudspeaker: admins ({%- for u in users.values() if u and 'admin' in u.roles %}<@{{ u.id }}>{% if not loop.last %},{% endif %}{% endfor -%})
{%- endif -%}
