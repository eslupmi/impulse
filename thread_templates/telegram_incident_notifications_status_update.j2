update: status <b>{% if incident.status == 'unknown' %}<a href="https://docs.impulse.bot/stable/warnings/StatusUnknown/">unknown</a>{% else %}{{ incident.status }}{% endif %}</b>{%- if incident.status == 'unknown' %}  |  🔔 admins ({%- for u in users.values() if u and 'admin' in u.roles %}<a href="tg://user?id={{ u.id }}">{{ u.name }}</a>{% if not loop.last %},{% endif %}{% endfor -%}){% endif %}
