name: _lint

on:
  workflow_call:
    inputs:
      runs-on:
        type: string
        default: self-hosted
      ref:
        type: string
        required: false

jobs:
  ruff:
    runs-on: ${{ inputs.runs-on }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref }}
      - name: Run ruff
        run: |
          docker run --rm \
            -e RUFF_CACHE_DIR=/tmp/ruff_cache \
            -e FORCE_COLOR=1 \
            -e TERM=xterm-256color \
            -v "${{ github.workspace }}:/app:ro,Z" \
            -w /app \
            python:3.12-slim \
            sh -c "
              python -m pip install --upgrade pip && \
              pip install ruff && \
              ruff check . --target-version py310 --exclude tests --exclude docs
            "

  mypy:
    runs-on: ${{ inputs.runs-on }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref }}
      - name: Run mypy
        run: |
          docker run --rm \
            -e FORCE_COLOR=1 \
            -e TERM=xterm-256color \
            -v "${{ github.workspace }}:/app:ro,Z" \
            -w /app \
            python:3.12-slim \
            sh -c "
              python -m pip install --upgrade pip && \
              pip install -r requirements.txt && \
              pip install mypy types-PyYAML types-icalendar types-requests && \
              printf '%s\n' '[mypy]' 'python_version = 3.10' 'exclude = (?x)(^tests/|^docs/)' 'plugins = pydantic.mypy' > /tmp/mypy.ini && \
              mypy --config-file /tmp/mypy.ini --cache-dir=/tmp/mypy_cache --color-output --pretty .
            "
