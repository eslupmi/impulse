name: _migration

on:
  workflow_call:
    inputs:
      branch:
        required: true
        type: string
      deploy-path:
        required: true
        type: string
    secrets:
      HOST:
        required: true
      PORT:
        required: true
      USER:
        required: true
      SSH_KEY:
        required: true

jobs:
  migration:
    runs-on: self-hosted
    steps:
      - name: Get latest release
        id: latest
        env:
          GH_TOKEN: ${{ github.token }}
        run: echo "tag=$(gh api "repos/${{ github.repository }}/releases/latest" --jq .tag_name)" >> "$GITHUB_OUTPUT"

      - name: Backup incidents migration
        uses: appleboy/ssh-action@v1.0.3
        env:
          DEPLOY_PATH: ${{ inputs.deploy-path }}
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: DEPLOY_PATH
          command_timeout: 15m
          script_stop: true
          script: |
            set -euo pipefail
            cp -a "$DEPLOY_PATH/docker-compose.yml" /tmp/impulse-migration-compose.yml
            for messenger in mattermost slack telegram; do
              REF=/tmp/impulse-migration-incidents-$messenger
              rm -rf "$REF"
              cp -a "$DEPLOY_PATH/$messenger/data/incidents" "$REF"
            done

      - name: Update incidents migration
        uses: appleboy/ssh-action@v1.0.3
        env:
          BRANCH: ${{ inputs.branch }}
          DEPLOY_PATH: ${{ inputs.deploy-path }}
          LATEST_RELEASE: ${{ steps.latest.outputs.tag }}
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: BRANCH,DEPLOY_PATH,LATEST_RELEASE
          command_timeout: 45m
          script_stop: true
          script: |
            set -euo pipefail
            sed -i "s|ghcr.io/eslupmi/impulse:[^ ]*|ghcr.io/eslupmi/impulse:${BRANCH}|" "$DEPLOY_PATH/docker-compose.yml"
            cd "$DEPLOY_PATH"
            for messenger in mattermost slack telegram; do
              echo "::group::migration update: $messenger"
              docker compose pull
              docker compose up -d
              sleep 30s
              docker compose stop
              docker compose run --rm --no-deps impulse-"$messenger" python -m main --downgrade "$LATEST_RELEASE"
              echo "::endgroup::"
            done

      - name: Compare incidents migration
        uses: appleboy/ssh-action@v1.0.3
        env:
          DEPLOY_PATH: ${{ inputs.deploy-path }}
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: DEPLOY_PATH
          command_timeout: 15m
          script_stop: true
          script: |
            set -euo pipefail
            for messenger in mattermost slack telegram; do
              echo "::group::migration compare: $messenger"
              REF=/tmp/impulse-migration-incidents-$messenger
              diff -ru "$REF" "$DEPLOY_PATH/$messenger/data/incidents"
              echo "::endgroup::"
            done

      - name: Restore impulse prod
        if: always()
        uses: appleboy/ssh-action@v1.0.3
        env:
          DEPLOY_PATH: ${{ inputs.deploy-path }}
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          envs: DEPLOY_PATH
          command_timeout: 15m
          script: |
            set -euo pipefail
            cd "$DEPLOY_PATH"
            docker compose stop || true
            COMPOSE_BAK=/tmp/impulse-migration-compose.yml
            for messenger in mattermost slack telegram; do
              REF=/tmp/impulse-migration-incidents-$messenger
              if [ -d "$REF" ]; then
                rm -rf "$DEPLOY_PATH/$messenger/data/incidents"
                cp -a "$REF" "$DEPLOY_PATH/$messenger/data/incidents"
              fi
              rm -rf "$REF"
            done
            if [ -f "$COMPOSE_BAK" ]; then
              cp -a "$COMPOSE_BAK" "$DEPLOY_PATH/docker-compose.yml"
            fi
            docker compose up -d
            rm -rf "$COMPOSE_BAK"
