name: "[m] autotests"

on:
  workflow_dispatch:
    inputs:
      pr_number:
        required: true
        type: number

jobs:
  autotests:
    uses: ./.github/workflows/_autotests.yml
    with:
      branch: pr-${{ inputs.pr_number }}
      runs-on: self-hosted
    secrets: inherit
