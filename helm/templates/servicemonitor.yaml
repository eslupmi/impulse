{{- if .Values.service.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "impulse.fullname" . }}
  labels:
    {{- include "impulse.labels" . | nindent 4 }}
    {{- with .Values.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "impulse.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: {{ .Values.service.port }}
      path: {{ .Values.service.path }}
      interval: {{ .Values.service.interval }}
      scrapeTimeout: {{ .Values.service.scrapeTimeout }}
      {{- with .Values.service.relabelings }}
      relabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.service.metricRelabelings }}
      metricRelabelings:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- with .Values.service.namespaceSelector }}
  namespaceSelector:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
